version: '3'

services:
  app:
    build: 
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "8080:8080"
    depends_on:
      - db
  db:
    image: postgres
    environment:
      POSTGRES_USER: shortcut
      POSTGRES_PASSWORD: shortcut
      POSTGRES_DB: shortcut
